<!--index.wxml-->
<view>
  <text>尊敬的：</text>
  <input value='{{guest}}' bindinput='updateGuest' />
  <button type='warn' bindtap='confirm' disabled='{{!guest}}'>确认</button>
</view>
<view>提示：身份一经确认，不可更改。</view>